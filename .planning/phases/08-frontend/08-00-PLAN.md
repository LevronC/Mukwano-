---
phase: 08-frontend
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/web/package.json
  - packages/web/vite.config.ts
  - packages/web/tsconfig.json
  - packages/web/tsconfig.app.json
  - packages/web/tsconfig.node.json
  - packages/web/index.html
  - packages/web/src/main.tsx
  - packages/web/src/index.css
  - packages/web/src/App.tsx
  - packages/web/src/vite-env.d.ts
  - packages/web/src/lib/utils.ts
  - packages/web/components.json
  - packages/web/vitest.config.ts
  - packages/web/src/test/setup.ts
  - packages/web/src/test/server.ts
  - packages/web/src/test/handlers.ts
  - packages/web/src/components/ui/button.tsx
  - packages/web/src/components/ui/input.tsx
  - packages/web/src/components/ui/card.tsx
  - packages/web/src/components/ui/badge.tsx
  - packages/web/src/components/ui/tabs.tsx
  - packages/web/src/components/ui/dialog.tsx
  - packages/web/src/components/ui/label.tsx
  - packages/web/src/components/ui/textarea.tsx
  - packages/web/src/components/ui/select.tsx
  - packages/web/src/components/ui/progress.tsx
  - packages/web/src/components/ui/separator.tsx
  - packages/web/src/components/ui/avatar.tsx
  - packages/web/src/components/ui/sonner.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "npm run build in packages/web succeeds with zero errors"
    - "npm run test in packages/web executes vitest and finds test stubs"
    - "Vite dev server starts and serves index.html at localhost"
    - "Tailwind v4 CSS classes compile correctly"
    - "shadcn/ui components are code-generated into src/components/ui/"
  artifacts:
    - path: "packages/web/vite.config.ts"
      provides: "Vite + React + Tailwind v4 + API proxy config"
      contains: "@tailwindcss/vite"
    - path: "packages/web/vitest.config.ts"
      provides: "Vitest + jsdom test configuration"
      contains: "jsdom"
    - path: "packages/web/src/test/handlers.ts"
      provides: "MSW v2 handlers for all 47 API endpoints"
      contains: "http.get"
    - path: "packages/web/src/test/server.ts"
      provides: "MSW server instance"
      contains: "setupServer"
    - path: "packages/web/src/index.css"
      provides: "Tailwind v4 CSS entry"
      contains: '@import "tailwindcss"'
  key_links:
    - from: "packages/web/vite.config.ts"
      to: "@tailwindcss/vite"
      via: "Vite plugin array"
      pattern: "tailwindcss\\(\\)"
    - from: "packages/web/vitest.config.ts"
      to: "packages/web/src/test/setup.ts"
      via: "setupFiles"
      pattern: "setupFiles.*setup\\.ts"
---

<objective>
Scaffold the entire packages/web Vite project from an empty package.json, install all dependencies, configure Tailwind v4 + shadcn/ui, set up Vitest + RTL + MSW v2 test infrastructure, and create MSW handlers for all 47 API endpoints.

Purpose: Every subsequent wave depends on this foundation existing. No screen can be built without the build tool, component library, and test infrastructure.
Output: A building, testable Vite + React + TypeScript project with all shadcn/ui components and complete test infrastructure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-frontend/08-CONTEXT.md
@.planning/phases/08-frontend/08-RESEARCH.md
@.planning/phases/08-frontend/08-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite project, install all dependencies, configure Tailwind v4 + shadcn/ui</name>
  <files>
    packages/web/package.json
    packages/web/vite.config.ts
    packages/web/tsconfig.json
    packages/web/tsconfig.app.json
    packages/web/tsconfig.node.json
    packages/web/index.html
    packages/web/src/main.tsx
    packages/web/src/App.tsx
    packages/web/src/index.css
    packages/web/src/vite-env.d.ts
    packages/web/src/lib/utils.ts
    packages/web/components.json
    packages/web/src/components/ui/*.tsx
  </files>
  <read_first>
    packages/web/package.json
    .planning/phases/08-frontend/08-RESEARCH.md (Standard Stack section + Installation section)
  </read_first>
  <action>
    1. Initialize Vite React-TS project in packages/web:
       ```bash
       cd packages/web && npm create vite@latest . -- --template react-ts
       ```
       If files conflict with existing package.json, overwrite — the current package.json is a stub.

    2. Install runtime dependencies:
       ```bash
       npm install @tanstack/react-query react-router-dom react-hook-form zod @hookform/resolvers sonner lucide-react clsx tailwind-merge class-variance-authority
       ```

    3. Install Tailwind CSS v4 (NOT v3 — use the Vite plugin, NOT PostCSS):
       ```bash
       npm install -D tailwindcss @tailwindcss/vite
       ```

    4. Configure vite.config.ts with Tailwind v4 Vite plugin AND API proxy:
       ```typescript
       import { defineConfig } from 'vite'
       import react from '@vitejs/plugin-react'
       import tailwindcss from '@tailwindcss/vite'
       import path from 'path'

       export default defineConfig({
         plugins: [react(), tailwindcss()],
         resolve: {
           alias: { '@': path.resolve(__dirname, './src') }
         },
         server: {
           proxy: {
             '/api': { target: 'http://localhost:3000', changeOrigin: true }
           }
         }
       })
       ```

    5. Replace src/index.css with Tailwind v4 CSS entry:
       ```css
       @import "tailwindcss";

       @theme {
         --color-primary: #3d6b4f;
         --color-primary-foreground: #ffffff;
         --color-accent: #f59e0b;
         --color-accent-foreground: #78350f;
       }
       ```

    6. Initialize shadcn/ui:
       ```bash
       npx shadcn@latest init
       ```
       When prompted: style=new-york, base-color=neutral, css-variables=yes, tailwind-config=use CSS variables, components-alias=@/components, utils-alias=@/lib/utils

    7. Install all required shadcn/ui components:
       ```bash
       npx shadcn@latest add button input card badge tabs dialog label textarea select progress separator avatar sonner
       ```

    8. Update src/main.tsx to be a minimal React 18 createRoot entry (no routing yet — that comes in Wave 1):
       ```tsx
       import React from 'react'
       import ReactDOM from 'react-dom/client'
       import App from './App'
       import './index.css'

       ReactDOM.createRoot(document.getElementById('root')!).render(
         <React.StrictMode><App /></React.StrictMode>
       )
       ```

    9. src/App.tsx should render a simple placeholder: `<div className="p-8"><h1 className="text-2xl font-bold text-primary">Mukwano</h1></div>`

    10. Verify build succeeds: `npm run build`
  </action>
  <verify>
    <automated>cd /Users/levicheptoyek/MUKWANO && npm run build --workspace=packages/web</automated>
  </verify>
  <acceptance_criteria>
    - packages/web/vite.config.ts contains `import tailwindcss from '@tailwindcss/vite'` and `tailwindcss()` in plugins array
    - packages/web/vite.config.ts contains `proxy: { '/api': { target: 'http://localhost:3000'`
    - packages/web/src/index.css contains `@import "tailwindcss"`
    - packages/web/src/lib/utils.ts exists and exports `cn` function
    - packages/web/src/components/ui/button.tsx exists
    - packages/web/src/components/ui/tabs.tsx exists
    - packages/web/src/components/ui/dialog.tsx exists
    - `npm run build --workspace=packages/web` exits with code 0
  </acceptance_criteria>
  <done>Vite project builds, Tailwind v4 compiles, all 13 shadcn/ui components exist in src/components/ui/</done>
</task>

<task type="auto">
  <name>Task 2: Install test dependencies, create Vitest config, MSW server, and all MSW handlers for 47 API endpoints</name>
  <files>
    packages/web/vitest.config.ts
    packages/web/src/test/setup.ts
    packages/web/src/test/server.ts
    packages/web/src/test/handlers.ts
  </files>
  <read_first>
    .planning/phases/08-frontend/08-VALIDATION.md
    .planning/phases/08-frontend/08-RESEARCH.md (MSW section + Test Framework section)
    .planning/phases/08-frontend/08-CONTEXT.md (API Surface section — all 47 endpoints)
  </read_first>
  <action>
    1. Install test dependencies:
       ```bash
       cd packages/web && npm install -D vitest @testing-library/react @testing-library/user-event @testing-library/jest-dom msw jsdom
       ```

    2. Create packages/web/vitest.config.ts:
       ```typescript
       import { defineConfig } from 'vitest/config'
       import react from '@vitejs/plugin-react'
       import path from 'path'

       export default defineConfig({
         plugins: [react()],
         resolve: { alias: { '@': path.resolve(__dirname, './src') } },
         test: {
           environment: 'jsdom',
           globals: true,
           setupFiles: ['./src/test/setup.ts'],
         },
       })
       ```

    3. Create packages/web/src/test/setup.ts:
       ```typescript
       import '@testing-library/jest-dom'
       import { server } from './server'
       import { beforeAll, afterEach, afterAll } from 'vitest'

       beforeAll(() => server.listen({ onUnhandledRequest: 'warn' }))
       afterEach(() => server.resetHandlers())
       afterAll(() => server.close())
       ```

    4. Create packages/web/src/test/server.ts:
       ```typescript
       import { setupServer } from 'msw/node'
       import { handlers } from './handlers'
       export const server = setupServer(...handlers)
       ```

    5. Create packages/web/src/test/handlers.ts with MSW v2 handlers for ALL 47 API endpoints.
       Use `http.get`, `http.post`, `http.patch`, `http.delete` from 'msw' and `HttpResponse` from 'msw'.
       Base path: `/api/v1`. Return realistic mock data shapes. Key handlers include:

       - POST /api/v1/auth/signup -> 201 { id, email, displayName, accessToken, refreshToken }
       - POST /api/v1/auth/login -> 200 { id, email, displayName, isGlobalAdmin, accessToken, refreshToken }
       - POST /api/v1/auth/logout -> 200 { message }
       - POST /api/v1/auth/refresh -> 200 { accessToken, refreshToken }
       - GET /api/v1/auth/me -> 200 { id, email, displayName, isGlobalAdmin, country, sector }
       - PATCH /api/v1/auth/me -> 200 { id, email, displayName }
       - GET /api/v1/config -> 200 { demoMode: true, currency: 'USD', escrowLabel: 'Simulated escrow' }
       - GET /api/v1/circles -> 200 [{ id, name, description, goalAmount, currency, status, createdAt }]
       - POST /api/v1/circles -> 201 { id, name, goalAmount }
       - GET /api/v1/circles/:id -> 200 { id, name, description, goalAmount, currency, status, governanceConfig: {...} }
       - PATCH /api/v1/circles/:id -> 200 { id, name }
       - POST /api/v1/circles/:id/join -> 201 { circleId, userId, role: 'member' }
       - POST /api/v1/circles/:id/leave -> 200 { message }
       - POST /api/v1/circles/:id/close -> 200 { id, status: 'closed' }
       - GET /api/v1/circles/:id/members -> 200 [{ circleId, userId, role, user: { id, email, displayName } }]
       - PATCH /api/v1/circles/:id/members/:userId/role -> 200 { circleId, userId, role }
       - POST /api/v1/circles/:id/contributions -> 201 { id, amount, status: 'pending' }
       - GET /api/v1/circles/:id/contributions -> 200 [{ id, amount, status, userId, note, createdAt }]
       - GET /api/v1/circles/:id/contributions/:cid -> 200 { id, amount, status }
       - PATCH /api/v1/circles/:id/contributions/:cid/verify -> 200 { id, status: 'verified' }
       - PATCH /api/v1/circles/:id/contributions/:cid/reject -> 200 { id, status: 'rejected' }
       - POST /api/v1/circles/:id/contributions/:cid/proof -> 200 { uploadUrl, fileKey, expiresInSeconds }
       - POST /api/v1/circles/:id/contributions/:cid/proof/confirm -> 201 { id, fileKey, fileName }
       - GET /api/v1/circles/:id/contributions/:cid/proof/:pid/view -> 200 { viewUrl }
       - GET /api/v1/circles/:id/treasury -> 200 { circleId, balance: 5000, currency: 'USD', balanceLabel: 'USD 5,000.00 (simulated)' }
       - GET /api/v1/circles/:id/ledger -> 200 [{ id, type, amount, runningBalance, createdAt }]
       - POST /api/v1/circles/:id/proposals -> 201 { id, title, description, requestedAmount, status: 'open' }
       - GET /api/v1/circles/:id/proposals -> 200 [{ id, title, status, requestedAmount, votingDeadline }]
       - GET /api/v1/circles/:id/proposals/:pid -> 200 { id, title, description, requestedAmount, status, votes: { yes, no, abstain, total }, myVote: null }
       - POST /api/v1/circles/:id/proposals/:pid/vote -> 201 { id, vote: 'yes' }
       - POST /api/v1/circles/:id/proposals/:pid/close -> 200 { id, status: 'closed_passed' }
       - DELETE /api/v1/circles/:id/proposals/:pid -> 200 { id, status: 'cancelled' }
       - POST /api/v1/circles/:id/projects -> 201 { id, proposalId, status: 'approved', budget: 1000 }
       - GET /api/v1/circles/:id/projects -> 200 [{ id, title, status, budget }]
       - GET /api/v1/circles/:id/projects/:projId -> 200 { id, title, status, budget, proposal: {...} }
       - PATCH /api/v1/circles/:id/projects/:projId -> 200 { id, status: 'executing' }
       - POST /api/v1/circles/:id/projects/:projId/updates -> 201 { id, content, percentComplete }
       - GET /api/v1/circles/:id/projects/:projId/updates -> 200 [{ id, content, percentComplete, createdAt }]
       - GET /api/v1/portfolio -> 200 [{ id, circleId, circleName, amount, status }]
       - GET /api/v1/portfolio/summary -> 200 { totalContributed: 5000, totalVerified: 3000, totalInProjects: 1000 }
       - GET /api/v1/dashboard -> 200 { circles: [], pendingContributions: 2, unvotedProposals: 1, recentActivity: [] }
       - GET /api/v1/admin/contributions/pending -> 200 [{ id, amount, circleName }]
       - GET /api/v1/admin/members -> 200 [{ id, email, displayName, isGlobalAdmin }]
       - PATCH /api/v1/admin/members/:id/role -> 200 { id, isGlobalAdmin: true }
       - GET /api/v1/admin/ledger -> 200 [{ id, type, amount, circleName }]
       - GET /api/v1/admin/activity -> 200 [{ id, type, description, createdAt }]

    6. Add a "test" script to packages/web/package.json: `"test": "vitest"`

    7. Verify test infrastructure works: `npx vitest --run` (should find 0 tests but exit cleanly)
  </action>
  <verify>
    <automated>cd /Users/levicheptoyek/MUKWANO/packages/web && npx vitest --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/web/vitest.config.ts exists and contains `environment: 'jsdom'`
    - packages/web/src/test/setup.ts exists and contains `import '@testing-library/jest-dom'`
    - packages/web/src/test/server.ts exists and contains `setupServer`
    - packages/web/src/test/handlers.ts exists and contains at least 40 `http.` handler registrations
    - packages/web/package.json contains `"test": "vitest"` in scripts
    - `npx vitest --run` exits without import/config errors
  </acceptance_criteria>
  <done>Vitest runs with jsdom, MSW v2 server starts, all 47 API endpoint handlers are registered, test infrastructure is ready for component tests in subsequent waves</done>
</task>

</tasks>

<verification>
- `npm run build --workspace=packages/web` exits 0
- `cd packages/web && npx vitest --run` exits without errors
- `grep -c 'http\.' packages/web/src/test/handlers.ts` returns >= 40
- `ls packages/web/src/components/ui/ | wc -l` returns >= 13
</verification>

<success_criteria>
- Vite project builds with zero TypeScript errors
- Tailwind v4 CSS compiles (text-primary class works)
- All shadcn/ui components are available in src/components/ui/
- Vitest + jsdom + MSW v2 test infrastructure is operational
- MSW handlers cover all 47 API endpoints with realistic mock shapes
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend/08-00-SUMMARY.md`
</output>
