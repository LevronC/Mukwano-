{
  "version": 2,
  "installCommand": "if [ -f ../../package.json ]; then (cd ../.. && npm install --no-audit --no-fund); else npm install --no-audit --no-fund; fi",
  "buildCommand": "npm run deploy:triggers && npm run vercel:migrate && npm run prisma:generate && node scripts/vercel-prisma-bundle.mjs",
  "outputDirectory": "public",
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "includeFiles": "src/**,prisma/**,node_modules/.prisma/**"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
