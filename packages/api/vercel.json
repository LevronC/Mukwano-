{
  "version": 2,
  "installCommand": "if [ -f ../../package.json ]; then (cd ../.. && npm install); else npm install; fi",
  "buildCommand": "npm run deploy:triggers && npm run vercel:migrate && npm run prisma:generate",
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "includeFiles": "src/**,prisma/**,node_modules/.prisma/**"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
