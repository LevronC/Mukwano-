export function Progress({ value = 0 }: { value?: number }) { return <div className='h-2 w-full rounded bg-gray-200'><div className='h-2 rounded bg-[#3d6b4f]' style={{ width: `${Math.max(0, Math.min(100, value))}%` }} /></div> }
